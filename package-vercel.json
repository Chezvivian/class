{
  "name": "aliyun-tts-vercel-proxy",
  "version": "1.0.0",
  "description": "阿里云TTS Vercel中间层代理",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
