{
  "name": "azure-tts-vercel-proxy",
  "version": "2.0.0",
  "description": "Azure Speech Service TTS Vercel中间层代理",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required for API project'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
